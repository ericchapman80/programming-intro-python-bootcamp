# Phase 01: First Python Program

## Goal

Run the first Python program.

## Learn

- What a `.py` file is
- `print()`
- Running Python from Terminal
- Syntax
- Error messages

## Build

Update `app/app.py` so it prints a welcome message for Track Career Analyzer.

## Run

```bash
cd app
python3 app.py
```

## Git Workflow

```bash
git switch -c phase-01-first-python
git status
git add .
git commit -m "Add first Python program"
git push -u origin phase-01-first-python
```

## Demo

- Run the program.
- Change the welcome message live.
- Explain what `print()` does.
- Explain one error you saw or one mistake you avoided.

